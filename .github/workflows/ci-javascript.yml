name: JavaScript CI

on:
  push:
    branches: [master]
  pull_request:
    branches:
    - '**'

steps:
- uses: actions/checkout@v4
- name: Use Node.js
  uses: actions/setup-node@v4
  with:
    node-version: 20.x
- run: npm ci
- run: npm run lint
